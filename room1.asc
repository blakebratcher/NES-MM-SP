// WARNING: this Room had a "Save/Load disabled" setting, which is now deprecated,
// and so it was removed during upgrade. If you like to restore this behavior,
// you would have to implement it in script. (This warning is safe to remove)

// room script file

function room_Load()
{
  aManiacintro.Play();
	gAction.Visible = false;
	gMaingui.Visible = false;  
	gBorder.Visible = false;
}

function hStart_AnyClick()
{
  StopMusic();
	Mouse.Visible = false;
  
  //Zum Spieler-Character wechseln
  //Beim Wechseln mit SetPlayer wird zusätzliche die Hintergrundmusik angepasst
  cSyd.SetPlayer();

	StopMusic();
}
